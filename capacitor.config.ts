import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cardapio-cachorrao',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
